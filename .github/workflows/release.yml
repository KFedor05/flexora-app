name: Release

# Triggers only on version tags like v1.0.0, v0.0.1, v2.3.4-rc1, etc.
# Push a tag to start a release; nothing else runs this workflow.
on:
  push:
    tags:
      - "v*.*.*"

# Allow only one release run at a time, regardless of tag.
concurrency:
  group: release
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.platform }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-22.04
            args: ""
          - platform: windows-latest
            args: ""
          - platform: macos-latest
            args: "--target universal-apple-darwin"

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          key: ${{ matrix.platform }}-release

      - name: Install Tauri Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libxdo-dev \
            build-essential \
            file

      - name: Install npm dependencies
        run: npm ci

      # Pull the section for this version out of CHANGELOG.md so the GitHub
      # release page shows the actual list of changes instead of a generic
      # "see the changelog" pointer. Falls back to an empty string if the
      # section is missing (release body still has the download links below).
      - name: Extract changelog section
        id: changelog
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          SECTION=$(awk -v v="$VERSION" '
            $0 ~ "^## \\["v"\\]" {flag=1; next}
            flag && /^## \[/ {exit}
            flag {print}
          ' CHANGELOG.md)
          {
            echo "body<<CHANGELOG_EOF"
            echo "$SECTION"
            echo "CHANGELOG_EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Build Tauri bundles and create draft release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "Flexora ${{ github.ref_name }}"
          releaseBody: |
            ${{ steps.changelog.outputs.body }}

            ---

            ## Downloads

            - **Windows:** `.exe` installer (NSIS) or `.msi`
            - **macOS:** `.dmg` (universal — Apple Silicon + Intel)
            - **Linux:** `.AppImage` (portable) or `.deb`

            > Windows: SmartScreen may warn on first launch because the build is not code-signed.
            > Click "More info" → "Run anyway". Code signing is planned for a later release.
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
